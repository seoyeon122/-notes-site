# 《四时小路观测周报》设计整改方案（去 AI 味 · 日式简约）

> 🔒 **内部维护文档**：设计方案稿，非面向读者的内容。

> 状态：方案稿（本阶段只产出方案，不改代码）
> 目标：降低"AI 味"，向**日式简约 / 编辑设计（anyway.fm 式）**收敛；体验"由浅入深"——初次接触便捷，深入后留下风格印象。
> 配套阅读：`design.md`（既有设计文档）、`decisions.md`（历史决策）

---

## 0. 先说清楚：什么是"AI 味"

"AI 味"不是某一个元素，而是一组**高频套路**叠加出来的观感。本项目里具体表现为：

| 症状 | 具体位置 | 为什么像 AI 生成 |
|---|---|---|
| **过度圆角 + 阴影** | 卡片 `border-radius:10px` + `box-shadow` 悬浮抬升 | "白卡浮在暗底上、hover 上浮"是模板站最经典的三件套 |
| **胶囊按钮遍地** | 多个 `border-radius:999px` 按钮 | 圆胶囊 = 现代 SaaS/仪表盘风格，和"都市传说传单"世界观冲突 |
| **纯色渐变当背景** | 电台版 `linear-gradient(#b27685→#beb09c)` | 无纹理、无质感的双色渐变，是 AI 出图/UI 模板的典型手法 |
| **通用动效** | 元素进场 `opacity 0→1 + y 28→0`、页面过渡"下坠/上浮" | 千篇一律的 fade-up / slide-up，没有"观测记录"该有的叙事感 |
| **emoji 式符号** | `↗`、`＋`、`→` | 通用箭头，缺乏设计语言 |
| **装饰性描边** | 虚线占位框「施工中」 | 未完成的"占位感"本身就很出戏 |

**核心判断**：世界观（都市传说传单）和配色基调（暗红黑 + 鲜红）是**对的、且很独特**，问题不在"做什么"，而在"怎么做的质感"。所以整改是**减法 + 质感替换**，不是推倒重来。

---

## 1. 设计原则（三条，贯穿所有阶段）

1. **传单质感（Zine / Print）**：想象它是"每周印一张、塞进门缝"的实物传单。版式要像纸面排版——对齐、留白、字号层级，而不是"网页卡片流"。
2. **克制的红**：鲜红 `#FF2231` 只用于**一处强调**（期号 / 关键数据 / 激活态），大面积铺红是灾难。其余靠**灰阶层次**说话。
3. **由浅入深**：
   - **浅层（第一屏 3 秒）**：能立刻知道"这是什么、最新一期在哪"
   - **中层（往下滚）**：版式节奏、留白、栏目感让人愿意停留
   - **深层（细看）**：细节（期号、报头、落款、信号灯语义）留下"这人审美不错"的印象

---

## 2. 阶段划分总览

| 阶段 | 名称 | 周期 | 目标 | 风险 |
|---|---|---|---|---|
| 一 | **去味（去 AI 痕迹）** | 1~2 天可上手 | 干掉最出戏的模板痕迹，视觉立刻"不像 AI 做的" | 低 |
| 二 | **立骨（版式与质感）** | 1~2 周 | 建立日式简约的排版骨架 + 纸张质感 | 中 |
| 三 | **入味（交互叙事）** | 长期 | 动效叙事化、信号灯语义化、体验由浅入深 | 高（依赖前两阶段） |

---

## 3. 阶段一：去味（立即可上手，低风险）

> 只改 CSS 数值/属性，不动结构、不动内容。改完立刻"顺眼"。

### 3.1 收圆角
- 卡片、速览卡、数据块：`border-radius` 从 `10px/8px` 统一收到 **`2px~4px`**（报纸/档案的直角感）。
- 胶囊按钮（CTA、期号徽章）：`999px` → **`2px` 或直角 + 细边框**。
- 信号灯圆点保留（那是世界观符号，不是 UI 圆角）。

### 3.2 减阴影、去悬浮抬升
- 删除卡片 `hover: translateY(-2px) + box-shadow` 的"浮起"动效。
- hover 反馈改为**更轻的**：边框/文字变红、或下划线，**不位移**。
- 全局 `--box-shadow` 重值保留只用于"报头"一类需要立体感的单一元素，其余移除。

### 3.3 电台版渐变 → 纯色 + 纹理
- 移除 `linear-gradient(#b27685→#beb09c)`。
- 改为**单一暖纸色**（如 `#e9e2d6` 或 `#f0e8dc`）+ 可选一层极淡的纸张噪点（CSS `repeating-*` 或 SVG feTurbulence 噪点图，或干脆纯色先上）。
- 渐变本身不是错，错在"裸渐变"；若想保留，必须叠纹理。**先做纯色**，最快见效。

### 3.4 符号规范化
- 统一把 `↗` 换成纯文字或自绘的细箭头 SVG（单线条、圆头、低存在感）。
- 删除 `＋`、`→` 等裸符号作为装饰。

### 3.5 "施工中"占位 → 有叙事感的占位
- 虚线框 + "施工中"太出戏。改为：**"未公开记录"** 或留空 + 一句小字「档案待解封」，弱化占位感（可后续阶段做，但文案先改）。

### 阶段一完成标准
首页 + 阅读页扫一眼，**不再出现"白卡浮在暗底"的模板观感**，圆角/阴影明显收敛。

---

## 4. 阶段二：立骨（版式与质感，1~2 周）

> 目标是建立"日式编辑排版"的骨架。这是整个整改的**核心**。

### 4.1 建立严格的排版栅格
- 参考 anyway.fm：**左侧一个固定的窄栏（报头/期号区），右侧内容主栏**。我们已有 80px 装订脊，把它升级为"**报头脊**"：不只放站名，放**期号、发刊日期、版次**等印刷信息，像报刊的书脊。
- 正文最大宽度收紧（当前 720px），配合更大**左右留白**，形成"纸面居中、两侧呼吸"的编辑感。
- 中文正文**行高放宽**到 `2.0~2.2`，段间距加大，弱化"网页紧排"感。

### 4.2 字号与字重层级（关键，日式简约的命门）
- **减少字号层级**：现在 h1/h2/h3 阶梯太多。收敛为 3 档：报头大字、栏目标题、正文。
- **标题用宋体细字重（≤400）**，正文用宋体常规；避免粗黑体大字（那是 AI 模板通病）。
- 期号、日期、统计数字用**等宽数字 `tabular-nums`** + 无衬线，形成"表格感"。

### 4.3 配色收敛（减法）
- 把暗底上的灰阶做成**有层次的暖灰**（`#e6e2d8` → `#b9b1ac` → `#8b8580`），减少"纯白 + 纯红"的硬对比。
- 鲜红只留 2~3 个触点：期号、激活导航、关键数据数字。
- 电台版（第二点五版）与第三版（往期观测）的浅色区，统一为**同一款暖纸色系**，避免各版各唱各调。

### 4.4 纸面质感（可选但很提气）
- 暗底加一层极淡的纸张噪点（全局背景），避免"死黑数码屏"。
- 浅色阅读页可考虑极淡的米色纸底（`#f6f4f0` 已是，再细化）。
- 关键分隔线从"纯色 1px"改为**双细线 / 装订线**，呼应"传单/档案"。

### 4.5 组件统一重绘（在立骨后逐一定稿）
- 报头 Masthead、速览卡、切片卡片、数据块、上下篇——统一到"细线 + 小圆角 + 无阴影 + 灰阶 + 红点缀"这一套语言下。

### 阶段二完成标准
把首页和阅读页拼成一张长图，**有"翻开一本日系独立杂志"的版式气质**，而非网页。

---

## 5. 阶段三：入味（交互叙事，长期）

> 前两阶段是"长得像"，这阶段是"用起来对味"。

### 5.1 动效叙事化（替换通用 fade-up）
- 进场动画从"整体 fade-up"改为**有顺序的编排**：报头先落定 → 期号 → 正文逐段浮现（像翻页）。
- 页面过渡从"下坠/上浮"改为**更像翻页/揭页**的转场（可探索：轻微纵向位移 + 淡入，速度放慢、加缓动）。
- 所有动效**降速**（0.9s→1.2s+），减少"蹦"的感觉。

### 5.2 信号灯语义化（把装饰变成功能）
- 红黄绿三灯不应该是摆设，赋予它状态：如"**本周有未读观测 = 红灯**""一切正常 = 绿灯"。
- 首页信号灯可做成"观测中"的呼吸灯（已有 breathing），语义统一。

### 5.3 由浅入深的信息节奏
- **浅层**：首页首屏只保留"站名 + 一句话 + 最新一期入口"，其余往下藏。
- **中层**：高亮卡 / 近期更新 / 往期列表，用节奏铺开。
- **深层**：阅读页的期号、报头、落款、特别感谢——细看有惊喜（如期号用特殊字体、落款盖"观测章"）。

### 5.4 微细节（长期积累）
- 落款可做成"印章"式图形。
- 期号用特殊字形（如旧式数字）。
- 光标/选中色定制。
- RSS/分享入口的低调化。

### 阶段三完成标准
第一次访问的人能 3 秒理解、30 秒逛完首页、点进阅读页后愿意看完一篇；老访客会注意到"每期期号、落款、信号灯"这些固定仪式感细节。

---

## 6. 优先级建议（我作为设计师的建议）

**先做阶段一全部 + 阶段二的 4.1/4.2/4.3**。理由：

1. 阶段一是"立刻不丢脸"——低成本去掉最明显的 AI 痕迹。
2. 4.1~4.3 是"气质命门"——排版栅格、字号层级、配色收敛，这三样决定"日系感"成不成立；反而 4.4 质感、4.5 组件重绘是锦上添花，可以后置。
3. 阶段三依赖前两阶段落地后的手感，不建议跳步。

**不建议现在做的**：WebGL 特效、重动效、印章图形等"深加工"——世界观已经够特别，先把版式基础打牢，质感自然上来。

---

## 7. 一个贯穿全程的验收方法

每次改完，用同一句"设计师自检"问自己：

> **把它打印成 A4 纸，贴在墙上，还像不像一个网页？**

像网页 → 还有 AI 味；像一张能贴墙上的传单/杂志 → 方向对了。

---

## 附：与既有决策的关系（不冲突的说明）

- 配色（暗红黑 + 鲜红 + 暖白）**保留**，阶段二是"收敛灰阶 + 红减量"，不是换色。
- 首页三版结构（封面 → 电台 → 往期）**保留**，阶段二是"每版内部版式重塑"。
- 左侧装订脊 → 升级为"报头脊"，仍是同一组件，只改其承载的信息密度。
- 阅读页固定浅色、汉堡菜单、近期更新栏等既有功能**全部保留**。
