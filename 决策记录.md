# 决策记录（做过什么 / 否过什么 / 为什么）

> 本项目的历史决策档案，供压缩上下文或换 AI 会话时快速找回"为什么"。
> ⚠️ **每次做重要决策（新增 / 否决 / 调整功能）后，回来补一条。**

---

## 项目定位

- **最终方向**：为 VTuber「四时小路 Komichi」的粉丝做周报网站，把合作朋友（B 站专栏"又一不举害我备孕失败"）的周报**重新排版收录**。
- **早期方向（已放弃）**：个人多分类笔记博客。用户提出更明确的目标后废弃。
- 合作模式：朋友写周报 → 我们每周重排版收录 → 每期保留署名 + 原文链接（**已获朋友授权**）。

## 技术栈

- **Astro 7 静态站**（免备案、无服务器、内容即 Markdown）。备选评估：Next.js（重）、手写 HTML（原始）。
- 动效：Lenis 平滑滚动 + GSAP ScrollTrigger（"动效地基"）；`data-animate` 属性约定，尊重系统 `prefers-reduced-motion`。
- 托管规划：Cloudflare Pages（免备案）；**域名未定**（部署时填 `astro.config.mjs` 的 `site`）。

## 设计决策

- **配色（反复试过，最终定案）**：
  1. 交通红 `#C8102E` + 纯白（anyway 编辑风）→ 早期
  2. 红黑渐变试验（暗色 / 浅色）→ 试过，浅色版"灰黑脏"被否
  3. **DeepSeek 金棕纸墨配色（风化宣纸 / 秋日金穗 / 夜雾蓝黑）→ ❌ 被否**：用户认为"AI 味太重"
  4. **定案：komichi-vup 暗红黑**——底 `#0C0505`、强调鲜红 `#FF2231`、文字暖白 `#F6EBE8`；"纸面模式"（暖白浅色）作为切换按钮的第二主题
- **明暗模式**：页头切换按钮（记忆 localStorage，防闪烁脚本在 BaseHead）；**默认暗色**；页头固定暗色报头（两模式一致）；凌晨四点卡固定暗色。
- **首页形态演变**：
  1. 一屏 hero → 两段式长页面（上半"传单" + 下半"往期观测"）
  2. 上半改"报告左右两页"：左封面 + 右**可滚动周报窗口**
  3. **定案**：去掉预览窗 → 右区"**两行高亮卡**"（第一行 = 最新一期，第二行 = 上一期；每行两张）；卡片 **4:3、图:文 = 3:1、左对齐**（用户确认与页头"首页"左缘"刚刚好"）；卡片之间 30px 空挡
- 交通信号灯（红 / 黄 / 绿）保留为装饰，呼应"凌晨四点的都市传说"世界观。
- **顶部栏 → 左侧栏（定案）**：56px 顶部横条改为 **80px 左侧"装订脊"**（fixed 贴左，内容整体右移 80px，内页略有偏移可接受）；脊上：竖排站名「四时小路」+ 竖排信号灯 + 导航（首页/归档/关于，**米灰 `#E6E2D8` 横排文字**，激活 = 红色左线）+ 明暗切换（按钮文字**「明 / 暗」**，不用 emoji）。原 Header/HeaderLink 组件已删除。
- **凌晨四点观测**：实时显示此刻哪个整点时区处于 04:00–04:59（东八区固定 = 中国 · 北京）；整点时区简化模型，极少数时刻两个时区并列。

## 内容与授权

- 已获朋友授权收录周报（第 1~3 期）；**仓库保持 Private**，公开 / 部署时机由用户决定。
- `参考文献/` 放朋友原文，仅本地（.gitignore，不入库）。
- 内容模型：`issues` 集合；frontmatter 含 `title / issue / pubDate / author / originalUrl / summary / highlights`（首页高亮卡数据，文章页顶部"本周速览"复用）。

## 技术优化（已做）

- **图片本地化**：B 站热链有 Referer 防盗链（localhost 会 403）→ 全站加 `no-referrer` + **19 张图下载到 `public/images/issue-N/`**，文章改本地路径，不再依赖 B 站 CDN。
- 移除未用的 Atkinson 字体（白下载）；OG 分享图改为海报背景图 `Background_test02.png`。
- 首页背景图 = `src/assets/Background_test02.png`（90% 不透明度，夜色调不压暗）。

## 待做 / 未决

- 域名：待用户定（部署时填）。
- 骨架升级剩余组件：TOC、数据大数字块、切片卡片（MDX）、时间线、上一篇 / 下一篇。
- 操作手册、AGENTS、本文件：每次新增功能后同步更新。

## 文档体系（三件套）

| 文件 | 给谁 | 作用 |
|---|---|---|
| `AGENTS.md` | AI 新会话 | 状态 / 红线 / Roadmap / 约定 |
| `操作手册.md` | 人类 | 无 AI 手操步骤 |
| `决策记录.md` | 两者 | "为什么"（本文件） |

**规则：新增功能 → 三份文档都要同步更新。**
