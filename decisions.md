# 决策记录（做过什么 / 否过什么 / 为什么）

> 本项目的历史决策档案，供压缩上下文或换 AI 会话时快速找回"为什么"。
> ⚠️ **每次做重要决策（新增 / 否决 / 调整功能）后，回来补一条。**

---

## 🏷️ v0.1 里程碑（2026-08 定稿）

功能全部齐活、**Vercel 已上线**、文档三件套完整。后续从「香港服务器部署 / 自定义域名 / 每周收录 / 细节打磨」任一项继续。

## 🏷️ v0.2 里程碑（2026-08 定稿）

**移动端专属排版（第一阶段完成）**：① 汉堡菜单导航（顶栏+下拉，含 ClientRouter 兼容修复）；② 早间电台版（第二点五版）上线（渐变底 + 4 图 + 收听按钮）。下一阶段大功能待定，文档（manual/decisions/AGENTS）已同步。

---

## 项目定位

- **最终方向**：为 VTuber「四时小路 Komichi」的粉丝做周报网站，把合作朋友（B 站专栏"又一不举害我备孕失败"）的周报**重新排版收录**。
- **早期方向（已放弃）**：个人多分类笔记博客。用户提出更明确的目标后废弃。
- 合作模式：朋友写周报 → 我们每周重排版收录 → 每期保留署名 + 原文链接（**已获朋友授权**）。

## 技术栈

- **Astro 7 静态站**（免备案、无服务器、内容即 Markdown）。备选评估：Next.js（重）、手写 HTML（原始）。
- 动效：Lenis 平滑滚动 + GSAP ScrollTrigger（"动效地基"）；`data-animate` 属性约定，尊重系统 `prefers-reduced-motion`。
- 托管规划（已定案）：**Vercel 已上线**（通过 GitHub 仓库自动部署）；实测大陆访问速度一般。**后续目标：阿里云/腾讯云香港服务器（免备案、国内可直连）**；暂不走大陆备案（2~4 周 + 身份信息，暂不必要）。

## 设计决策

- **配色（反复试过，最终定案）**：
  1. 交通红 `#C8102E` + 纯白（anyway 编辑风）→ 早期
  2. 红黑渐变试验（暗色 / 浅色）→ 试过，浅色版"灰黑脏"被否
  3. **DeepSeek 金棕纸墨配色（风化宣纸 / 秋日金穗 / 夜雾蓝黑）→ ❌ 被否**：用户认为"AI 味太重"
  4. **定案：komichi-vup 暗红黑**——底 `#0C0505`、强调鲜红 `#FF2231`、文字暖白 `#F6EBE8`；"纸面模式"（暖白浅色）作为切换按钮的第二主题
- **明暗模式**：~~页头切换按钮（记忆 localStorage，防闪烁脚本在 BaseHead）~~ → **已移除切换功能（2026 定案）**：删除 `ThemeToggle.astro` 组件与 BaseHead 的 localStorage 主题脚本（提交 e1fc9a1 之后），首页/归档/404 固定暗色；**阅读页（`/issue/`、`/about`）固定浅色**（`IssueLayout` 的 `<html data-theme="light">` 自声明，与切换无关，保留）。两套配色变量仍在 `global.css`（`:root` 暗色 / `:root[data-theme='light']` 浅色）；**双模式切换后续再做**（git 历史可找回旧实现）。
- **首页形态演变**：
  1. 一屏 hero → 两段式长页面（上半"传单" + 下半"往期观测"）
  2. 上半改"报告左右两页"：左封面 + 右**可滚动周报窗口**
  3. **定案**：去掉预览窗 → 右区"**两行高亮卡**"（第一行 = 最新一期，第二行 = 上一期；每行两张）；卡片 **4:3、图:文 = 3:1、左对齐**（用户确认与页头"首页"左缘"刚刚好"）；卡片之间 30px 空挡
  4. **早间电台版（第二点五版，2026 施工中）**：插入在第一版与第三版之间；渐变底 `#b27685→#beb09c`（顶藕粉→底暖米，用户提供色号）；左右切分与第一版一致（`calc(50% + 160px)`）；左区电台介绍（占位文案"每周一清晨…"）+ 右区 **4 个正方形空占位（无圆角、虚线框、标「施工中」）**，后续填电台图片。文案/图片待用户提供。**2026-08 将 `.radio` 高度从 `100svh` 提升至 `120svh`**：滚动到第三版时增加余量，避免第一版残留；不做整页吸附切换（此前已否决）。
- 交通信号灯（红 / 黄 / 绿）保留为装饰，呼应"凌晨四点的都市传说"世界观。
- **顶部栏 → 左侧栏（定案）**：56px 顶部横条改为 **80px 左侧"装订脊"**（fixed 贴左，内容整体右移 80px，内页略有偏移可接受）；脊上：竖排站名「四时小路」+ 竖排信号灯 + 导航（首页/归档/关于，**米灰 `#E6E2D8` 横排文字**，激活 = 红色左线）+ 明暗切换（按钮文字**「明 / 暗」**，不用 emoji）。原 Header/HeaderLink 组件已删除。
- **移动端响应式（定案）**：≤820px 时左侧栏自动变**顶部横栏**（站名横排、导航横排、激活改红色下划线、切换按钮靠右、信号灯隐藏），正文让位从 `padding-left:80px` 切到 `padding-top:60px`（`--rail-h` 变量），进度条/锚点同步让位；首页高亮卡 ≤480px 改单列。纯 CSS 媒体查询实现，无 JS；桌面布局不受影响。
- **移动端导航 → 汉堡菜单（2026 定案）**：≤820px 时顶栏改为"站名常驻 + 右侧汉堡按钮（44px 触控区，距右缘 15px）"，导航（首页/归档/关于）收进下拉菜单（滑出动画、三横线变 ✕、激活项红色文字）。桌面端仍为左侧装订脊。用事件委托 + `data-burger-ready` 幂等标记 + `astro:page-load` 重绑，规避 ClientRouter 切页后脚本不重跑、以及 `astro:page-load` 首载也触发导致双重绑定互相抵消（点开又立即关上、看似无响应）的坑。
- **首页"近期更新"栏（2026 定案）**：第一版左栏"阅读最新一期"按钮下方，仿"往期观测"列表格式（红字日期 + 暖白标题 + 暗色分隔线）。**数据驱动**：`src/pages/index.astro` 顶部 `recentUpdates` 数组加一条即多一行（date/title/href）；周报更新 href 用 `/issue/issue-N/`、电台更新用 `#radio`（早间电台版，`scroll-margin-top:80px` 锚点偏移避开顶栏）。维护流程见 manual.md §2.5。
- **周报阅读页固定浅色（解耦定案）**：`IssueLayout` 的 `<html>` 自声明 `data-theme="light"`（浅色纸面底 + 深墨字，不随全局暗色主题变）；BaseHead 主题脚本尊重页面自声明；阅读页（`/issue/`、`/about`）隐藏明暗切换按钮。首页与阅读页从此独立调整。
- **页面过渡（定案）**：默认叠化 → 自定义"**旧页向下渐隐 → 新页向上渐显**"（0.38s / 0.45s，位移 26px，`global.css` 底部 `vt-fade-*`）；**左侧栏 `view-transition-name: rail` 单独快照保持不动**（曾试 `transition:persist`，有"明/暗"按钮跨页不刷新的副作用，弃用）。
- **凌晨四点观测**：实时显示此刻哪个整点时区处于 04:00–04:59（东八区固定 = 中国 · 北京）；整点时区简化模型，极少数时刻两个时区并列。
- **整页滚动（fullpage/scroll snap）→ ❌ 评估后否决**：用户想"滚一下切一版"（液化）。评估风险：①与 Lenis 平滑滚动冲突（需禁用 Lenis 或手写吸附）；②仅首页需要、需按路由隔离；③移动端内容溢出会被吸附卡住；④与锚点/页面过渡/无障碍联动复杂。结论：收益低、风险高，**暂不做**；先试"版面相隔加间距"折中版，用户看后也否决（"算了，回撤"），最终维持版面紧贴。

## 内容与授权

- 已获朋友授权收录周报（第 1~3 期）；**仓库保持 Private**，公开 / 部署时机由用户决定。
- `参考文献/` 放朋友原文，仅本地（.gitignore，不入库）。
- 内容模型：`issues` 集合；frontmatter 含 `title / issue / pubDate / author / originalUrl / summary / highlights`（首页高亮卡数据，文章页顶部"本周速览"复用）。

## 技术优化（已做）

- **图片本地化**：B 站热链有 Referer 防盗链（localhost 会 403）→ 全站加 `no-referrer` + **19 张图下载到 `public/images/issue-N/`**，文章改本地路径，不再依赖 B 站 CDN。
- 移除未用的 Atkinson 字体（白下载）；OG 分享图改为海报背景图 `Background_test02.png`。
- 首页背景图 = `src/assets/Background_test02.png`（90% 不透明度，夜色调不压暗）。

## 待做 / 未决

- 域名：待用户定（部署时填）。
- 骨架升级进度：速览卡（高亮卡）✅ → **TOC 文章目录 ✅（升级为 Timeline Scrollspy 时间线目录）**：右侧固定灰色小线段（每章节一根），滚动时当前段放大 + 变浅灰高亮，悬停展开全部章节名（当前章节大字号），点击平滑跳转；窄屏（<1200px）隐藏。→ **上一篇/下一篇 ✅**（文末左右各一，红色钝角箭头 + 第N期 + 周期；首期无上一篇、最新期无下一篇；新增 `period` 周期字段，归档/首页往期同步显示）→ **本周数据块 ✅**（frontmatter `stats` 键值对 → 文章页大数字卡，红字大数字 + 灰标签）→ **切片卡片 ✅（骨架升级全部完成）**：`Clip.astro` 组件，正文视频链接卡片化（▶+标题+↗）；第 3 期转为 `.mdx` 样板（loader pattern 补上 `{md,mdx}`）。
- 操作手册、AGENTS、本文件：每次新增功能后同步更新。

## 文档体系（三件套）

| 文件 | 给谁 | 作用 |
|---|---|---|
| `AGENTS.md` | AI 新会话 | 状态 / 红线 / Roadmap / 约定 |
| `manual.md` | 人类 | 无 AI 手操步骤 |
| `decisions.md` | 两者 | "为什么"（本文件） |

**规则：新增功能 → 三份文档都要同步更新。**
