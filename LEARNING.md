# 学习路线图（边学边做版）

这是为你（刚入行的游戏策划，零工程经验）定制的学习地图。**跟着项目走，每一步都有真实产出。**

## 一、你的起点（先认清自己）

- 编程概念 ✅：会 C++ 最基础的知识（变量、循环、函数、逻辑）
- 网页感知 ✅：用 AI 做过 HTML 游戏原型，见过 HTML/CSS/JS 长什么样
- AI 协作 ✅：已经会用 Claude for VS Code 辅助开发
- 工程流程 ⬜：终端命令、npm、项目结构、git、部署——**这是网站搭建的主体，也是本路线图的重点**

结论：你不缺编程能力，缺的是工程流程。而工程流程恰好是"可以手把手教"的东西。

## 二、术语表（遇到不懂先查这里）

| 术语 | 是什么 | 类比 |
|---|---|---|
| **Node.js** | 让 JavaScript 能在电脑上运行的"运行时" | 像游戏引擎的运行环境 |
| **npm** | 装包工具，`npm install` 把依赖装进 `node_modules/` | 应用商店 |
| **终端 / 命令行** | 用文字命令操作电脑 | 游戏的控制台 |
| **项目结构** | 一个工程的文件怎么组织 | 项目的文件夹规划 |
| **Astro** | 本网站用的框架：编译 Markdown/组件 → 纯静态 HTML | 一条"原料→成品"的流水线 |
| **Markdown** | 用符号标记格式的纯文本（`#`标题、`**`加粗） | 轻量排版语言 |
| **frontmatter** | 文章顶部的 `---` 块，写标题/日期/分类等元信息 | 物品的属性面板 |
| **内容集合（Content Collections）** | Astro 管理一批同结构内容（本项目的文章）的方式 | 数据库的"表" |
| **静态网站** | 构建时生成好全部 HTML，访问时直接返回 | 打印好的成品书 |
| **构建（build）** | 把源码编译成最终网站的过程 | 把策划案做成可玩的版本 |
| **git** | 记录文件每次修改历史的工具 | 存档系统 |
| **部署（deploy）** | 把网站文件放到服务器上让全网能访问 | 发布版本 |
| **域名 + DNS** | 网址（如 example.com）和它指向服务器的"通讯录" | 门牌号 + 导航 |
| **备案** | 中国大陆服务器的实名审核（2~4 周） | 营业许可。我们用免备案的海外托管绕开它 |

## 三、学习路线图（五个阶段）

### 阶段一：跑起来（你已经完成 ✅）
- 理解：Node、npm、终端是什么
- 产出：`npm run dev` 后在 http://localhost:4321 看到网站
- 动手题：改 `src/consts.ts` 里的网站名，刷新看变化

### 阶段二：收录周报（进行中 ▶️）
- 理解：Markdown、frontmatter、内容集合
- 产出：把每周的 B 站周报重排成 `src/content/issues/` 下的一个 `.md` 文件
- 动手题：复制 `templates/周报模板.md`，改期数和正文，观察归档页自动出现新一期

### 阶段三：改样子
- 理解：Astro 组件、页面路由、CSS
- 产出：改首页布局、文章页样式、导航栏
- 动手题：把 `src/components/Header.astro` 的导航加一个链接（比如 B 站主页）

### 阶段四：部署上线
- 理解：git 版本控制、构建产物、静态托管
- 产出：网站部署到 Cloudflare Pages（免备案），获得一个公开网址
- 动手题：推送一次修改，看网站自动更新

### 阶段五：长期运营
- 理解：RSS、SEO、数据统计（如 Cloudflare Analytics）
- 产出：固定更新节奏 + 订阅功能 + 访问数据
- 动手题：给自己定一个"每周一更"计划

## 四、文件地图（每个文件是干什么的）

| 文件 | 作用 | 你要改它的频率 |
|---|---|---|
| `src/consts.ts` | 网站名、简介、编辑署名 | 偶尔 |
| `src/content.config.ts` | 周报字段定义（期数/日期/原文链接） | 很少 |
| `src/content/issues/*.md` | **每周收录的周报** | 每周 |
| `src/pages/index.astro` | 首页（最新一期 + 往期列表） | 偶尔 |
| `src/pages/archive.astro` | 全部期数归档 | 很少 |
| `src/pages/issue/[...slug].astro` | 单期页面（动态路由） | 很少 |
| `src/layouts/IssueLayout.astro` | 期数页布局（含阅读进度条） | 偶尔 |
| `src/components/Header.astro` | 顶部导航 | 偶尔 |
| `src/components/Footer.astro` | 页脚 | 很少 |
| `src/styles/global.css` | 全局样式（红黑 + 交通色） | 偶尔 |
| `astro.config.mjs` | Astro 配置 | 几乎不 |

**核心原则：周报内容在 `content/issues/`，结构在 `pages/`，外观在 `components/` + `styles/`。想改什么，先想清楚它属于哪一层。**

## 五、常见问题排查

| 现象 | 原因 | 解法 |
|---|---|---|
| `npm run dev` 报"astro: 不是内部或外部命令" | 没装依赖 | 先 `npm install` |
| 构建报 frontmatter 错误 | 文章少写了必填字段 | 对照 README 的字段表 |
| 新增周报归档没出现 | 保存的文件不是 `.md`，或文件在错误目录 | 确认在 `src/content/issues/` 下 |
| 端口被占用 | 4321 已被别的程序使用 | 看终端提示的实际端口 |
